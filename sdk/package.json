{
  "name": "agent-board",
  "version": "0.1.0",
  "description": "Zero-dependency Node SDK for the Agent Collab Board Markdown state system: read, apply (validated writes), watch, and op vocabulary behind a single createBoard(repo) API.",
  "main": "index.js",
  "types": "index.d.ts",
  "bin": {
    "agent-board": "bin/cli.js"
  },
  "files": [
    "index.js",
    "index.d.ts",
    "lib/",
    "bin/",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "markdown",
    "board",
    "project-management",
    "agent",
    "state"
  ],
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {},
  "scripts": {
    "check": "node --check index.js && node --check bin/cli.js && node --check lib/parse.js && node --check lib/state.js && node --check lib/write.js && node --check lib/guard.js && node --check lib/watch.js",
    "test": "node --test test/api.test.js test/cli.test.js"
  }
}
